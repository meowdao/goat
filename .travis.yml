language: node_js
node_js:
    - "0.10"
services:
  - mongodb
before_install:
  - npm install -g grunt-cli
  - npm install -g bower
install:
  - bundle install
  - npm install
  - bower install
script:
  - grunt travis



